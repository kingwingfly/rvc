/target

# OS cruft
*DS_Store
._*

# Python (only the ONNX-export helper under export/)
__pycache__/
.venv/

# Trained + downloaded model assets, training scratch
/models
/dataset
*.onnx

# Upstream repos cloned as porting references, read but never run.
/.reference

# Agent scratch worktrees
/.claude/worktrees

# LibTorch, for the `tch` backend. Never vendored: it is ~3 GB and
# platform/CUDA-specific, so each machine downloads its own and points
# LIBTORCH at it (see the LibTorch section of the README).
/libtorch
/libtorch*.zip
